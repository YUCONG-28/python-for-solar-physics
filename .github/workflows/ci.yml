name: Public CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  secret-history-scan:
    name: Gitleaks (complete history)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out complete repository history
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Scan for secrets
        uses: gitleaks/gitleaks-action@dcedce43c6f43de0b836d1fe38946645c9c638dc # v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_ENABLE_COMMENTS: false
          GITLEAKS_CONFIG: .gitleaks.toml

  python-public-package:
    name: Python public package
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Python
    env:
      PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: Python/pyproject.toml

      - name: Install package and test tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip install ruff

      - name: Compile public Python sources
        run: python -m compileall -q solar_toolkit tests examples/public_api

      - name: Run Ruff
        run: python -m ruff check solar_toolkit tests examples/public_api

      - name: Run public Python tests
        run: python -m pytest tests

      - name: Build wheel
        run: python -m build --wheel --outdir dist

      - name: Validate wheel boundary
        shell: bash
        run: |
          python - <<'PY'
          from configparser import ConfigParser
          from glob import glob
          from io import StringIO
          from zipfile import ZipFile

          wheels = sorted(glob("dist/*.whl"))
          if len(wheels) != 1:
              raise SystemExit(f"expected one wheel, found {wheels}")

          blocked_prefixes = (
              "solar_toolkit/webapp/",
              "solar_toolkit/visualization/image_web_viewer/",
              "solar_toolkit/visualization/_media_assets/",
              "solar_toolkit/visualization/media_assets/",
              "solar_toolkit/radio/configs/",
              "scripts/",
              "legacy/",
              "Local/",
          )
          blocked_suffixes = (".html", ".css", ".js")

          with ZipFile(wheels[0]) as archive:
              names = archive.namelist()
              forbidden = [
                  name
                  for name in names
                  if name.startswith(blocked_prefixes)
                  or (
                      name.startswith("solar_toolkit/")
                      and name.lower().endswith(blocked_suffixes)
                  )
              ]
              if forbidden:
                  raise SystemExit(f"local application content in wheel: {forbidden}")

              entry_points = [
                  name for name in names if name.endswith(".dist-info/entry_points.txt")
              ]
              for entry_point in entry_points:
                  parser = ConfigParser()
                  parser.read_file(
                      StringIO(archive.read(entry_point).decode("utf-8"))
                  )
                  if parser.has_section("console_scripts"):
                      raise SystemExit(
                          f"public console scripts remain: {dict(parser['console_scripts'])}"
                      )

          print(f"wheel boundary ok: {wheels[0]}")
          PY

          wheel_env="${RUNNER_TEMP}/solarphysics-wheel-smoke"
          python -m venv --system-site-packages "$wheel_env"
          "$wheel_env/bin/python" -m pip install --force-reinstall --no-deps dist/*.whl
          (
            cd "${RUNNER_TEMP}"
            "$wheel_env/bin/python" -I -c \
              "from pathlib import Path; import sys; import solar_toolkit; import solar_toolkit.radio; assert Path(solar_toolkit.__file__).is_relative_to(Path(sys.prefix)); print(solar_toolkit.__version__)"
          )

  paper-evidence-layer:
    name: Paper generator and indexes
    runs-on: windows-latest
    defaults:
      run:
        shell: powershell
    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Generate Paper outputs without live search
        run: .\Paper\scripts\paper_daily_recommendation.ps1 -SkipLiveSearch

      - name: Parse Paper JSON and CSV sources
        run: |
          $config = Get-Content -Raw .\Paper\config\paper_search_config.json | ConvertFrom-Json
          $seeds = @(Get-Content -Raw .\Paper\data\seed_papers.json | ConvertFrom-Json)
          $master = @(Import-Csv .\Paper\paper_master_index.csv)
          $gaussian = @(Import-Csv .\Paper\02_methods_gaussian_fitting\gaussian_fitting_paper_index.csv)

          if ($null -eq $config.output_paths) { throw "Paper config has no output_paths" }
          if ($seeds.Count -eq 0) { throw "Paper seed data is empty" }
          if ($master.Count -eq 0) { throw "Paper master index is empty" }
          if ($gaussian.Count -eq 0) { throw "Paper Gaussian index is empty" }

          Write-Host "Paper JSON/CSV validation passed: seeds=$($seeds.Count), master=$($master.Count), gaussian=$($gaussian.Count)"

      - name: Run Paper Pester tests
        run: powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "Invoke-Pester -Script .\Paper\tests\PaperRecommendation.Tests.ps1 -EnableExit"
