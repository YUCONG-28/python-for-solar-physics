# Changelog

## Unreleased

- Reorganized standalone workflows under `scripts/` by scientific task.
- Moved reusable helpers into `solar_toolkit/`.
- Added optional YAML path configuration via `solar_toolkit.path_config`.
- Added `configs/paths.example.yaml` for local data path templates.
- Split lightweight tests into `tests/` and local-data examples into `examples/`.
- Added GitHub Actions lightweight CI, contribution guidance, citation metadata, and documentation pages.
