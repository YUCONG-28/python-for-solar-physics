"""Application orchestration built on the reusable :mod:`solar_toolkit` API.

Workflow packages may depend on ``solar_apps.platform`` and ``solar_toolkit``.
They deliberately do not import frontend implementations.
"""
