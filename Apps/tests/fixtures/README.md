# Test fixtures

Only small, deterministic synthetic fixtures belong here. Observation files,
generated products, saved UI state, logs, and machine-specific paths must stay
in the ignored `Local/` runtime tree.
