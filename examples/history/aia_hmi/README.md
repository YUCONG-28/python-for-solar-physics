# Historical AIA/HMI recipes

These files preserve network-backed or tutorial-style workflows for reference.
They are not imported by `solar_toolkit` and are not installed in the wheel.

`time_distance_diagram_legacy.py` is the historical SunPy gallery-derived
recipe. The old script path remains a thin source-checkout launcher.
